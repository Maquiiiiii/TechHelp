# ==============================================================================
# Frontend — Variables de Entorno para PRODUCCIÓN (Netlify)
# ==============================================================================
# ⚠️  NUNCA subas este archivo a Git.
# En Netlify: Site configuration → Environment variables → Add variable
# ==============================================================================

# URL pública de tu backend en Railway (sin barra final, sin /api/v1)
# Reemplaza esta URL con la que te asigne Railway al desplegar el backend
VITE_API_URL=https://TU-BACKEND.up.railway.app/api/v1
